function createLight(root, color = 'red', size = 50,  time = 2000) {

}

const createLight=(document.querySelector('body'), 'blue', 80 , 20, 2000) => {
    const lampToggle = () =>{
        lamp.style.backgroundColor - isActive ? "grey" : color;
        isActive = !isActive;
    }
    const lamp = document.createElement("div");
    lamp.setAttribute()
}
